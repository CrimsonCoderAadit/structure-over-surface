// Neutral chart palette, deliberately separate from the three edge-type colors —
// those are reserved for content that actually refers to AST edge types.
export const CHART_COLORS = {
  dim: "#4a4d57",
  mid: "#9a9ca5",
  bright: "#e8e6e1",
  highlight: "#4fd1c5",
  grid: "#23262e",
};

export const chartFontFamily = "var(--font-mono)";
