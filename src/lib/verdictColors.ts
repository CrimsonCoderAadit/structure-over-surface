// Verdict colors for the classifier's human/machine call — deliberately distinct
// from the three edge-type colors (edgeTypes.ts), which mean something else.
export const VERDICT_COLORS = {
  human: "#6ee7b7",
  machine: "#fb923c",
} as const;
